# Assets

This folder stores local static assets used by helper workflows.

Typical contents include:

- `audio/` for local audio assets used during augmentation or offline experiments
- future static visuals if the project adds bundled image assets

The main app can run without large committed assets in this folder.
